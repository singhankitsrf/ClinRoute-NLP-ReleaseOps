# Release Metrics

Do not commit fabricated candidate metrics. For a real release, compare approved and candidate evaluation JSON with `scripts/model_regression_gate.py`. Test fixtures are not model-performance claims.
